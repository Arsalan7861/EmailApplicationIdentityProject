@model List<Category>

<div class="inbox-status">
    <ul class="inbox-st-nav">
        @foreach (var item in Model)
        {
            <li><a href="/Message/MessageCategory/@item.CategoryId"><i class="@item.CategoryIconUrl"></i> @item.CategoryName</a></li>
        }
    </ul>
</div>